# Copy to .env.local and fill in your values.

# Production URL, used for SEO metadata (sitemap, robots, Open Graph).
# Defaults to https://gebrye-portfolio.vercel.app if unset.
NEXT_PUBLIC_SITE_URL=
